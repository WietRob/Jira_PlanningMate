{
  "name": "jira-planning-mate",
  "version": "1.0.0",
  "description": "AI-powered PI Planning with Feasibility Certificate and Conflict Resolution",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:functions && npm run build:ui",
    "build:functions": "esbuild --bundle --outfile=dist/index.js --platform=node --target=node20 --external:@forge/manifest src/index.ts",
    "build:ui": "cd ui && npm install --legacy-peer-deps && npm run build && rm -rf ../static/* && cp -r dist/* ../static/",
    "test": "jest --passWithNoTests",
    "dev": "npm run build && forge tunnel --watch",
    "deploy": "npm run build && forge deploy",
    "install": "npm install && cd ui && npm install"
  },
  "dependencies": {
    "@forge/api": "^2.8.0",
    "@forge/bridge": "^2.6.0",
    "@forge/ui": "^1.9.0",
    "graphql": "^16.8.0"
  },
  "devDependencies": {
    "@babel/core": "^7.28.5",
    "@babel/preset-env": "^7.28.5",
    "@babel/preset-typescript": "^7.28.5",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.10.0",
    "babel-jest": "^30.2.0",
    "esbuild": "^0.19.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  }
}
